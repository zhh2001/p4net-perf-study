# Runtime dependencies. p4net is pinned exactly — the measurement
# methodology requires a fixed black-box version. Other deps use lower
# bounds.
p4net==1.7.0
psutil>=5.9
numpy
pandas
pyyaml
scapy>=2.5

# Development / measurement test dependencies.
pytest>=7
pytest-asyncio>=0.23
ruff>=0.5
